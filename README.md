# repose

a setup which allows agents to learn based on position encoding: the model re-weights context by relevance instead of fixed token order, so distant or buried evidence gets effective attention.

inspired by [repo](https://github.com/SakanaAI/repo).
